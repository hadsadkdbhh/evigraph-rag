# The current FinQA-300 local-planner pipeline uses only the Python standard
# library. Keep this file so clean-checkout instructions can always run:
#   python -m pip install -r requirements.txt
#
# Future GPU/dense-retrieval experiments can add optional dependencies in a
# separate requirements-gpu.txt instead of making the core reproducibility gate
# depend on CUDA or provider SDKs.
